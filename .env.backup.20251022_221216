# Database Configuration
# For local development, use your local PostgreSQL
DATABASE_URL=postgresql://candraczapansky@localhost:5432/devbooking_db?sslmode=disable
DB_HOST=localhost
DB_PORT=5432
DB_NAME=devbooking_db
DB_USER=candraczapansky
DB_PASSWORD=

# JWT Secret (generate a secure random string)
JWT_SECRET=dev_jwt_secret_change_in_production_$(openssl rand -hex 32)

# Application Configuration
NODE_ENV=development
PORT=3002
LOG_LEVEL=INFO
ENVIRONMENT=development
APP_ENCRYPTION_KEY=dev_encryption_key_$(openssl rand -hex 32)

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Twilio Configuration (optional - for SMS features)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# OpenAI Configuration (optional - for AI features)
OPENAI_API_KEY=your_openai_api_key

# SendGrid Configuration (optional - for email features)
SENDGRID_API_KEY=your_sendgrid_api_key
SENDGRID_FROM_EMAIL=noreply@yourdomain.com
SENDGRID_FROM_NAME=Your Business Name

# Helcim Payment Processing (optional - for payment features)
HELCIM_API_TOKEN=your_helcim_api_token
HELCIM_API_URL=https://api.helcim.com/v2
HELCIM_WEBHOOK_VERIFIER_TOKEN=your_verifier_token
VITE_HELCIM_ACCOUNT_ID=your_helcim_account_id
VITE_HELCIM_TERMINAL_ID=your_helcim_terminal_id
VITE_HELCIM_JS_TOKEN=your_helcim_js_token

# AWS S3 Configuration (optional - for file uploads)
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1
AWS_S3_BUCKET=your_s3_bucket_name

# Redis Configuration (optional - for caching)
REDIS_URL=redis://localhost:6379

# Business Configuration
BUSINESS_NAME=Your Salon Name
BUSINESS_PHONE=+1234567890
BUSINESS_EMAIL=info@yoursalon.com

# System Admin Credentials (will be created on first run)
ADMIN_EMAIL=admin@yoursalon.com
ADMIN_PASSWORD=Admin123!

# Disable automatic service creation (recommended for initial setup)
DISABLE_AUTOMATIC_SERVICE_CREATION=true
